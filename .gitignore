# --- Build artifacts ---
build/
cmake-build-*/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
*.o
*.a
*.so
*.so.*
*.dylib
*.exe
*.out

# --- Editor / tooling ---
compile_commands.json
.cache/
.clangd/
.vscode/
.idea/
*.swp
*~

# --- Models (large binaries, never commit) ---
models/*.gguf
models/*.bin
models/*.safetensors
models/*.onnx

# --- Principal-provided reference material (disposable; never committed) ---
code_and_concept/
reference/
dragoncache_intel/

# --- Pilot materials (LiNa_Behind_the_Scenes): decks, videos, audio, reports —
#    137 MB of media for the pilot; kept on the machine, never committed ---
LiNa_Behind_the_Scenes/

# --- Environment / secrets ---
.env
.env.*
*.pem
*.key

# --- Her private workspace (D-040) — runtime state, never committed ---
workspace/

# --- DragonCache generated artifacts (D-044) ---
.dragoncache_map

# --- Database dumps ---
*.sql.gz
*.dump
