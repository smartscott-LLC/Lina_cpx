# --- Build artifacts ---
build/
cmake-build-*/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
*.o
*.a
*.so
*.so.*
*.dylib
*.exe
*.out

# --- Editor / tooling ---
compile_commands.json
.cache/
.clangd/
.vscode/
.idea/
*.swp
*~

# --- Models (large binaries, never commit) ---
models/*.gguf
models/*.bin
models/*.safetensors
models/*.onnx

# --- Principal-provided reference material (disposable; never committed) ---
code_and_concept/
reference/

# --- Environment / secrets ---
.env
.env.*
*.pem
*.key

# --- Her private workspace (D-040) — runtime state, never committed ---
workspace/

# --- Database dumps ---
*.sql.gz
*.dump
